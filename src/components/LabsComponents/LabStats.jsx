import React from 'react';

const LabStats = (className) => {
  return (
    <div className={`${className} flex items-center  justify-center`}>
      <h1>LabStats</h1>
    </div>
  );
};

export default LabStats;    
